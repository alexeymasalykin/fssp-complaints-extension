services:
  license-server:
    build: .
    container_name: rkl-license-server
    restart: unless-stopped
    ports:
      - "3101:3101"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/licenses.db
    volumes:
      - license-data:/app/data

volumes:
  license-data:
