# Техническое задание: Chrome-расширение для автоматизации проверки по РКЛ через Госуслуги

**Версия:** 1.0  
**Дата:** 03.03.2026  
**Рабочее название продукта:** RKL Check

---

## 1. Описание продукта

Браузерное расширение для Google Chrome (и Chromium-браузеров: Яндекс.Браузер, Edge), автоматизирующее массовую проверку иностранных сотрудников по Реестру контролируемых лиц (РКЛ) МВД через портал Госуслуг.

Расширение работает **локально в браузере** пользователя. Персональные данные сотрудников **не покидают контур организации** --- передаются только на Госуслуги через авторизованную сессию, без промежуточных серверов.

**Бизнес-модель:** подписка (SaaS). Расширение распространяется через Chrome Web Store (unlisted --- по прямой ссылке). Функционал активируется лицензионным ключом, привязанным к подписке.

---

## 2. Целевая аудитория

- HR-специалисты и кадровики организаций, нанимающих иностранных граждан
- Компании со штатом от 10 до 1000+ иностранных сотрудников
- Отрасли: строительство, логистика, производство, клининг, общепит, сельское хозяйство

---

## 3. Конкурентный анализ

### 3.1. Карта конкурентов

**HRonBoard** (hronboard.ru)

- Тип: Chrome-расширение, автоматизация через Госуслуги
- Модель: 350 000 руб разово, без подписки, без ограничений
- Требует ЭП-ключ
- Дополнительно: автозаполнение уведомлений МВД о приёме/увольнении
- Данные локально, не передаются третьим лицам
- Проверка за 12 секунд
- Установка из магазина расширений Chrome/Яндекс

**RKLScan** (rklscan.ru)

- Тип: веб-сервис с личным кабинетом
- Модель: подписка + цена за проверку
  - XS: бесплатно, 2 руб/проверка
  - S: 790 руб/мес, 1.7 руб/проверка
  - M: 1 690 руб/мес, 1.5 руб/проверка
  - L: 2 790 руб/мес, 1.3 руб/проверка
  - XL: 4 490 руб/мес, 1.1 руб/проверка
- 10 бесплатных запросов при регистрации
- Данные уходят на сервер RKLScan --- 152-ФЗ распространяется
- Дисклеймер: "не являемся государственным органом, данные из открытых источников"
- Оповещения на почту при нахождении в РКЛ

**IshHub** (ishhub.pro)

- Тип: платформа аутсорсинга мигрантов
- Модель: процент от суммы заказанных услуг, проверка РКЛ бесплатно
- Загрузка Excel с ФИО и датами рождения
- Заявляют что данные не сохраняются после выгрузки
- Не конкурент напрямую --- РКЛ побочная фича, основной бизнес --- аутсорсинг

**Контур.Экстерн** (kontur.ru)

- Тип: бухгалтерский сервис
- Модель: входит в подписку на Экстерн
- Автоматическая проверка при отправке уведомлений в МВД
- Не отдельный продукт --- фича внутри экосистемы Контура
- Нельзя купить только проверку РКЛ

**api-cloud.ru**

- Тип: API-прослойка к сайту МВД (парсинг)
- Модель: 0.30 руб/запрос + 700 руб/мес минимальный платёж
- Только API, нет интерфейса
- Договор с ИП Кочура А.В.
- По договору вся ответственность по 152-ФЗ на клиенте
- Хранит данные до 72 часов

### 3.2. Сравнительная таблица

| Параметр | RKL Check (мы) | HRonBoard | RKLScan | api-cloud |
|---|---|---|---|---|
| Тип | Расширение Chrome | Расширение Chrome | Веб-сервис | API |
| Модель оплаты | Подписка/мес | 350 000 руб разово | Подписка + за проверку | За запрос |
| Порог входа | 2 990 руб/мес | 350 000 руб | от 790 руб/мес + 1.7 руб/шт | от 700 руб/мес + 0.30 руб/шт |
| Данные на стороннем сервере | Нет | Нет | Да | Да |
| 152-ФЗ ответственность | Только клиент --- Госуслуги | Только клиент --- Госуслуги | Клиент + RKLScan | Клиент + api-cloud |
| Источник данных | Госуслуги (МВД) | Госуслуги (МВД) | Открытые источники | Сайт МВД (парсинг) |
| Требует ЭП | Нет | Да | Нет | Нет |
| Скорость проверки | ~12 сек | ~12 сек | Не указана | ~6 сек |

### 3.3. Наши конкурентные преимущества

1. Порог входа в 70-100 раз ниже HRonBoard (2 990 руб vs 350 000 руб)
2. Данные не уходят третьим лицам (в отличие от RKLScan и api-cloud)
3. Не требуется ЭП-ключ (в отличие от HRonBoard)
4. Источник --- Госуслуги/МВД (официальный, в отличие от "открытых источников" RKLScan)
5. Подписочная модель --- можно отказаться в любой момент, нет lock-in на 350к
6. Не нужна разработка на стороне клиента (в отличие от api-cloud)

---

## 4. Архитектура

### 4.1. Обзор компонентов

Система состоит из двух независимых частей:

**Часть 1 --- Chrome-расширение (клиентская):**

- Popup UI --- интерфейс управления (загрузка данных, запуск, результаты)
- Content Script --- робот, работающий на странице Госуслуг (заполнение форм, чтение результатов)
- Background Service Worker --- координатор (очередь, состояние, связь между popup и content script)

**Часть 2 --- Сервер лицензий (серверная):**

- Проверка и активация лицензионных ключей
- Счётчик использования
- Управление тарифами и подписками
- Интеграция с платёжной системой

### 4.2. Схема взаимодействия

```
+-----------------------------------------------------+
|              Браузер пользователя                     |
|                                                       |
|  +---------------+    +---------------------------+   |
|  |  Popup UI     |    |  Content Script           |   |
|  |               |    |  (gosuslugi.ru/655781/*)  |   |
|  |  - Загрузка   |    |                           |   |
|  |    Excel/XML  |    |  - Заполнение полей       |   |
|  |  - Очередь    |<-->|  - Клик Продолжить        |   |
|  |  - Прогресс   |    |  - Чтение результата      |   |
|  |  - Результаты |    |  - Клик Проверить ещё     |   |
|  |  - Экспорт    |    |                           |   |
|  +-------+-------+    +-----------+---------------+   |
|          |                        |                    |
|  +-------+------------------------+----------------+   |
|  |           Background Service Worker              |   |
|  |  - Управление очередью проверок                  |   |
|  |  - Хранение результатов (chrome.storage.local)   |   |
|  |  - Проверка лицензии                             |   |
|  |  - Инкремент счётчика                            |   |
|  +------------------------+------------------------+   |
+---------------------------|----------------------------+
                            | HTTPS (только лицензия)
                +-----------+-----------+
                |   Сервер лицензий     |
                |                       |
                |  - Проверка ключа     |
                |  - Счётчик проверок   |
                |  - Управление тарифом |
                |  - Никаких ПДн        |
                +-----------------------+
```

### 4.3. Принцип работы content script

Расширение **не перехватывает API-запросы** Госуслуг и **не эмулирует HTTP-запросы**. Оно работает исключительно через DOM: заполняет поля формы, нажимает кнопки, считывает текст результата. Это полный эквивалент ручной работы кадровика, но автоматизированный.

Госуслуги используют React. Страница проверки РКЛ --- это SPA (Single Page Application). URL не меняется между шагами формы: `https://www.gosuslugi.ru/655781/1/form`. Переходы между шагами происходят через перерисовку DOM без перезагрузки страницы.

### 4.4. Разделение данных

**На сервере лицензий хранятся ТОЛЬКО:**

- ID клиента (организации)
- Лицензионный ключ (сгенерированный UUID)
- Название тарифа и дата окончания подписки
- Счётчик выполненных проверок за текущий период
- device_id для привязки ключа к устройству
- Email для связи

**Никаких персональных данных сотрудников. Никогда. Ни в каком виде.**

**Локально в браузере (chrome.storage.local / IndexedDB):**

- Загруженные данные сотрудников из Excel/XML (до закрытия сессии или ручной очистки пользователем)
- Результаты проверок текущей сессии
- История проверок (опционально, включается пользователем)
- Настройки расширения (задержка между проверками, формат экспорта)
- Кэш лицензии (для работы при кратковременной потере связи с сервером)

---

## 5. Подробное описание Госуслуг --- страница проверки РКЛ

### 5.1. URL и навигация

- Адрес сервиса: `https://www.gosuslugi.ru/655781/1/form`
- Тип: SPA на React
- URL не меняется при переходе между шагами формы
- Авторизация: пользователь должен быть залогинен на Госуслугах как сотрудник организации (без ЭП, обычная учётка)
- В шапке отображается "Организациям" --- подтверждение что сессия от имени юрлица

### 5.2. Шаги формы

**Шаг 1 --- Данные документа**

Заголовок страницы: "Укажите данные документа, удостоверяющего личность проверяемого иностранца"

Поля:
- "Серия" --- текстовое поле, подпись "При наличии" (необязательное)
- "Номер" --- текстовое поле, подпись "Как в графе Passport No документа" (обязательное)
- "Дата выдачи" --- поле даты с иконкой календаря, подпись "Как в графе Issue date документа" (обязательное)

Розовый блок-предупреждение: "Проверьте данные иностранца. Они должны точно совпадать с документом, удостоверяющим его личность"

Кнопка: "Продолжить"

**Шаг 2 --- Дата рождения**

Заголовок: "Укажите дату рождения проверяемого иностранца"

Поля:
- "Дата рождения" --- поле даты с иконкой календаря (обязательное)
- Чекбокс: "В документе неполная дата рождения"

Розовый блок-предупреждение: аналогичный шагу 1

Кнопка: "Продолжить"

**Шаг 3 --- Результат**

Варианты ответа:
- "Отсутствует в реестре контролируемых лиц" (с зелёной иконкой паспорта)
- Текст при нахождении в РКЛ --- точная формулировка уточняется при тестировании (предположительно "Состоит в реестре контролируемых лиц")

Информация в результате:
- Документ: [серия+номер], выдан [дата]
- Дата рождения: [дата]
- Данные получены из системы МВД России [дата] [время] МСК

Кнопки:
- "Проверить ещё" --- возврат к шагу 1 (без перезагрузки страницы)
- "На главную"

### 5.3. Технические наблюдения

- Фронтенд на React (подтверждено по DevTools: ошибки React-компонентов lib-date-picker)
- Поля ввода --- React controlled components (простое присвоение value не активирует обработчики React)
- Время ответа МВД: от 5 до 30 секунд (на тесте 03.03.2026: ответ за ~10 сек)
- Капча отсутствует (авторизованная сессия)
- Нет видимого rate limit при ручном использовании

---

## 6. Пользовательский сценарий (User Flow)

### 6.1. Первый запуск

1. Пользователь оплачивает подписку на лендинге
2. Получает лицензионный ключ (email + страница подтверждения)
3. Переходит по ссылке в Chrome Web Store, устанавливает расширение
4. Открывает popup расширения (клик по иконке в toolbar)
5. Вводит лицензионный ключ, нажимает "Активировать"
6. Расширение отправляет ключ + device_id на сервер
7. Сервер подтверждает: подписка активна, тариф "Бизнес", лимит 6000, использовано 0
8. Интерфейс расширения разблокирован

### 6.2. Подготовка к проверке

1. Пользователь авторизуется на gosuslugi.ru как сотрудник организации
2. Переходит на `gosuslugi.ru/655781/1/form` (или расширение может открыть эту страницу автоматически по кнопке "Открыть Госуслуги")
3. Открывает popup расширения
4. Загружает файл с данными сотрудников (drag-drop область или кнопка "Выбрать файл")
5. Расширение парсит файл (SheetJS для Excel, DOMParser для XML)
6. Отображает таблицу предпросмотра: N строк, колонки серия/номер/дата выдачи/дата рождения/ФИО
7. Подсвечивает ошибки: пустые обязательные поля, некорректный формат дат
8. Показывает информацию: "Найдено N сотрудников. Лимит: использовано X из Y"

### 6.3. Формат входных данных

**Excel (.xlsx) --- основной формат:**

Обязательные колонки (определяются по заголовку первой строки):

| Колонка | Обязательность | Формат | Примечание |
|---|---|---|---|
| Серия | Нет | Текст/число | Может быть пустой |
| Номер | Да | Текст/число | Как в графе Passport No |
| Дата выдачи | Да | ДД.ММ.ГГГГ | Как в графе Issue date |
| Дата рождения | Да | ДД.ММ.ГГГГ | |
| ФИО | Нет | Текст | Не передаётся в Госуслуги, только для отчёта |

Расширение должно уметь распознавать колонки по нескольким вариантам названий:
- "Номер" / "Номер документа" / "Passport No" / "docnum"
- "Дата выдачи" / "Issue date" / "docdate"
- "Дата рождения" / "birthdate" / "ДР"
- "Серия" / "Series"
- "ФИО" / "Имя" / "Name" / "fio"

**XML (выгрузка из 1С ЗУП) --- дополнительный формат (Фаза 4):**

Формат определяется после анализа стандартной выгрузки 1С ЗУП. Предполагаемая структура:

```xml
<Employees>
  <Employee>
    <Name>Акимали Уулу Кылымбек</Name>
    <BirthDate>20.05.2000</BirthDate>
    <DocSeries>ID</DocSeries>
    <DocNumber>4390174</DocNumber>
    <DocIssueDate>07.03.2025</DocIssueDate>
  </Employee>
</Employees>
```

### 6.4. Процесс проверки --- подробный алгоритм

**Предпроверки:**

1. Активная вкладка содержит URL `gosuslugi.ru/655781`? Если нет --- показать предупреждение "Откройте страницу проверки РКЛ на Госуслугах" с кнопкой "Открыть"
2. Лицензия активна? Если нет --- показать "Подписка истекла" с кнопкой "Продлить"
3. Лимит не исчерпан? Если исчерпан --- показать "Лимит проверок исчерпан (X из X). Повысьте тариф"
4. Файл загружен и валиден? Если нет --- показать ошибки валидации

**Цикл проверки (для каждого сотрудника):**

```
1. ОПРЕДЕЛЕНИЕ ШАГА
   - Прочитать текст заголовков на странице
   - Если "Укажите данные документа" -> шаг 1
   - Если "Укажите дату рождения" -> шаг 2
   - Если "реестре контролируемых лиц" -> шаг результата
   - Если ни один не определён -> ожидание 1 сек, повтор (до 30 сек)

2. ШАГ 1: ЗАПОЛНЕНИЕ ДОКУМЕНТА
   - Найти input для "Серия" -> заполнить через setReactInput()
   - Найти input для "Номер" -> заполнить
   - Найти input для "Дата выдачи" -> заполнить
   - Пауза 300-500 мс (имитация человека)
   - Найти кнопку "Продолжить" -> клик
   - Ожидание перехода к шагу 2 (до 10 сек)

3. ШАГ 2: ЗАПОЛНЕНИЕ ДАТЫ РОЖДЕНИЯ
   - Найти input для "Дата рождения" -> заполнить через setReactInput()
   - Пауза 300-500 мс
   - Найти кнопку "Продолжить" -> клик
   - Ожидание результата (до 120 сек --- МВД отвечает медленно)

4. ШАГ 3: ЧТЕНИЕ РЕЗУЛЬТАТА
   - Найти текст результата в DOM
   - Определить: "Отсутствует" = not_found, иначе = found
   - Считать дополнительные данные (документ, дата, время)
   - Отправить результат в background worker
   - Пауза 3-5 сек (настраиваемая, имитация человека)
   - Найти кнопку "Проверить ещё" -> клик
   - Ожидание возврата к шагу 1 (до 10 сек)

5. ИНКРЕМЕНТ И ПЕРЕХОД
   - Background worker отправляет increment на сервер лицензий
   - Background worker обновляет прогресс в popup
   - Переход к следующему сотруднику -> пункт 1
```

**Обработка ошибок:**

| Ошибка | Действие |
|---|---|
| Элемент не найден (таймаут) | Пометить сотрудника как "ошибка", перейти к следующему |
| Кнопка "Продолжить" неактивна | Повторить заполнение полей, если не помогло --- ошибка |
| Госуслуги показали ошибку | Скриншот текста ошибки в лог, повторная попытка (до 3 раз) |
| Сессия Госуслуг истекла | Остановить проверку, показать "Авторизуйтесь заново" |
| Потеря связи с сервером лицензий | Продолжить работу (кэш лицензии), синхронизировать счётчик позже |
| Пользователь закрыл вкладку | Остановить проверку, сохранить промежуточные результаты |

### 6.5. Управление проверкой

- **Пауза** --- остановить после текущего сотрудника, не начинать следующего
- **Продолжить** --- возобновить с того места где остановились
- **Остановить** --- прервать полностью, результаты сохранены
- **Повторить ошибочные** --- запустить проверку заново только для сотрудников со статусом "ошибка"

### 6.6. Просмотр результатов и экспорт

Popup отображает таблицу результатов:

| ФИО (если есть) | Документ | Статус | Время |
|---|---|---|---|
| Акимали Уулу К. | ID4390174 | Не найден | 17:50 |
| Иванов И.И. | 376336373663 | **НАЙДЕН В РКЛ** | 17:51 |
| Петров П.П. | 22068950515 | Ошибка | 17:52 |

Цветовая индикация:
- Зелёный фон --- не найден в РКЛ
- Красный фон --- НАЙДЕН в РКЛ
- Серый фон --- ошибка проверки

**Экспорт в Excel:**

Кнопка "Скачать отчёт" генерирует .xlsx файл (SheetJS) с колонками:
- Все исходные данные из загруженного файла
- Статус РКЛ (Не найден / Найден / Ошибка)
- Дата и время проверки (МСК)
- Источник: "Госуслуги / МВД России"
- Примечание (текст ошибки, если была)

Строки с найденными в РКЛ выделены красным фоном.
Строки с ошибками выделены жёлтым фоном.

---

## 7. Техническая спецификация

### 7.1. Структура файлов расширения

```
rkl-check/
  manifest.json              -- Manifest V3
  popup/
    popup.html               -- UI расширения
    popup.js                 -- Логика popup (загрузка, управление, результаты)
    popup.css                -- Стили popup
  content/
    gosuslugi.js             -- Content script для gosuslugi.ru/655781/*
  background/
    service-worker.js        -- Background service worker
  lib/
    xlsx.min.js              -- SheetJS для парсинга/генерации Excel
    license.js               -- Модуль проверки лицензии
    crypto.js                -- HMAC-верификация ответов сервера
  icons/
    icon16.png
    icon48.png
    icon128.png
  _locales/
    ru/
      messages.json          -- Локализация
```

### 7.2. manifest.json

```json
{
  "manifest_version": 3,
  "name": "RKL Check --- Проверка по реестру контролируемых лиц",
  "description": "Автоматическая массовая проверка иностранных сотрудников по РКЛ МВД через Госуслуги",
  "version": "1.0.0",
  "default_locale": "ru",
  "permissions": [
    "activeTab",
    "storage",
    "notifications",
    "tabs"
  ],
  "host_permissions": [
    "https://www.gosuslugi.ru/655781/*",
    "https://api.rklcheck.ru/*"
  ],
  "content_scripts": [
    {
      "matches": ["https://www.gosuslugi.ru/655781/*"],
      "js": ["content/gosuslugi.js"],
      "run_at": "document_idle"
    }
  ],
  "background": {
    "service_worker": "background/service-worker.js"
  },
  "action": {
    "default_popup": "popup/popup.html",
    "default_icon": {
      "16": "icons/icon16.png",
      "48": "icons/icon48.png",
      "128": "icons/icon128.png"
    }
  },
  "icons": {
    "16": "icons/icon16.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  }
}
```

### 7.3. Content Script --- gosuslugi.js (подробная спецификация)

**Заполнение React-полей:**

React использует synthetic events и контролируемые компоненты. Прямое присвоение element.value не вызовет обновление state компонента. Необходимо использовать нативный setter и dispatch событий:

```javascript
function setReactInput(element, value) {
  // Используем нативный setter чтобы обойти React controlled component
  const nativeInputValueSetter = Object.getOwnPropertyDescriptor(
    window.HTMLInputElement.prototype, 'value'
  ).set;
  nativeInputValueSetter.call(element, value);

  // Dispatch событий в правильном порядке
  element.dispatchEvent(new Event('focus', { bubbles: true }));
  element.dispatchEvent(new Event('input', { bubbles: true }));
  element.dispatchEvent(new Event('change', { bubbles: true }));
  element.dispatchEvent(new Event('blur', { bubbles: true }));
}
```

**Для полей даты (lib-date-picker):**

Госуслуги используют кастомный компонент lib-date-picker. Возможно потребуется:
- Заполнение через прямой ввод в текстовое поле (если доступно)
- Или эмуляция клика по календарю + выбор даты
- Точный способ определяется при разработке после инспекции DOM

**Ожидание элементов (MutationObserver):**

```javascript
function waitForElement(selector, textContent, timeout = 30000) {
  return new Promise((resolve, reject) => {
    // Сначала проверяем, есть ли элемент уже
    const existing = findElement(selector, textContent);
    if (existing) return resolve(existing);

    const observer = new MutationObserver(() => {
      const el = findElement(selector, textContent);
      if (el) {
        observer.disconnect();
        resolve(el);
      }
    });
    observer.observe(document.body, {
      childList: true,
      subtree: true,
      characterData: true
    });
    setTimeout(() => {
      observer.disconnect();
      reject(new Error(`Timeout: element ${selector} not found in ${timeout}ms`));
    }, timeout);
  });
}

function findElement(selector, textContent) {
  if (textContent) {
    const elements = document.querySelectorAll(selector);
    return Array.from(elements).find(el => el.textContent.includes(textContent));
  }
  return document.querySelector(selector);
}
```

**Определение текущего шага:**

```javascript
function getCurrentStep() {
  const bodyText = document.body.innerText;
  if (bodyText.includes('Укажите данные документа')) return 'document';
  if (bodyText.includes('Укажите дату рождения')) return 'birthdate';
  if (bodyText.includes('реестре контролируемых лиц')) return 'result';
  return 'unknown';
}
```

**Чтение результата:**

```javascript
function readResult() {
  const bodyText = document.body.innerText;
  const found = !bodyText.includes('Отсутствует в реестре');
  // Извлекаем дополнительные данные
  const dateMatch = bodyText.match(/(\d{2}\.\d{2}\.\d{4}\s+\d{2}:\d{2}\s+МСК)/);
  return {
    found: found,
    timestamp: dateMatch ? dateMatch[1] : new Date().toLocaleString('ru-RU'),
    source: 'Госуслуги / МВД России'
  };
}
```

**Случайные задержки (имитация человека):**

```javascript
function humanDelay(minMs = 300, maxMs = 800) {
  const delay = minMs + Math.random() * (maxMs - minMs);
  return new Promise(resolve => setTimeout(resolve, delay));
}
```

### 7.4. Background Service Worker

**Состояние (state machine):**

```
IDLE -> READY -> RUNNING -> PAUSED -> RUNNING -> COMPLETED
                    |                                |
                    +-----> ERROR -------> IDLE <----+
```

**Хранение данных:**

```javascript
// chrome.storage.local
{
  license: {
    key: "ABC123-DEF456-...",
    deviceId: "generated-uuid",
    plan: "business",
    limit: 6000,
    used: 47,
    expires: "2026-04-03T00:00:00Z",
    lastValidated: "2026-03-03T17:50:00Z"
  },
  queue: {
    employees: [...],     // массив данных сотрудников
    currentIndex: 5,      // текущая позиция
    results: [...],       // результаты проверок
    state: "running",     // idle/ready/running/paused/completed/error
    startedAt: "...",
    pausedAt: null
  },
  settings: {
    delayBetweenChecks: 4000,  // мс между проверками (3000-10000)
    resultTimeout: 120000,      // таймаут ожидания результата
    maxRetries: 3,              // макс повторов при ошибке
    autoExport: false,          // автоэкспорт по завершении
    keepHistory: true           // хранить историю проверок
  }
}
```

**Обмен сообщениями:**

| Источник | Назначение | Тип сообщения | Данные |
|---|---|---|---|
| Popup | Background | START_CHECK | {employees: [...]} |
| Popup | Background | PAUSE_CHECK | {} |
| Popup | Background | RESUME_CHECK | {} |
| Popup | Background | STOP_CHECK | {} |
| Popup | Background | GET_STATUS | {} |
| Background | Content | FILL_DOCUMENT | {series, number, issueDate} |
| Background | Content | FILL_BIRTHDATE | {birthDate} |
| Background | Content | READ_RESULT | {} |
| Background | Content | CLICK_CHECK_MORE | {} |
| Content | Background | STEP_DETECTED | {step: "document/birthdate/result"} |
| Content | Background | RESULT_READY | {found, timestamp, source} |
| Content | Background | ERROR | {message, step} |
| Background | Popup | STATUS_UPDATE | {state, current, total, results} |

### 7.5. Popup UI --- подробное описание экранов

**Экран 1: Активация лицензии**

- Логотип + название "RKL Check"
- Поле ввода: "Лицензионный ключ"
- Кнопка: "Активировать"
- Ссылка: "Нет ключа? Получить на rklcheck.ru"
- При ошибке: красный текст "Недействительный ключ" или "Подписка истекла"

**Экран 2: Главный (ожидание)**

- Информационная панель:
  - Тариф: Бизнес
  - Лимит: 47 / 6 000
  - Действует до: 03.04.2026
- Область загрузки файла (drag-drop):
  - Иконка файла + текст "Перетащите Excel или нажмите для выбора"
  - Поддерживаемые форматы: .xlsx, .xls, .xml
- Кнопка: "Открыть Госуслуги" (если вкладка не на нужной странице)

**Экран 3: Предпросмотр**

- Таблица загруженных данных (первые 5-10 строк + "и ещё N...")
- Счётчик: "Загружено: 100 сотрудников"
- Предупреждения валидации (если есть)
- Кнопка: "Начать проверку" (зелёная, крупная)
- Кнопка: "Отменить" (серая)

**Экран 4: Проверка идёт**

- Прогресс-бар: "Проверено: 47 из 100"
- Текущий статус: "Проверяется: документ 22068950515..."
- Статистика в реальном времени:
  - Не найдено в РКЛ: 45
  - Найдено в РКЛ: 1
  - Ошибки: 1
- Кнопки: "Пауза" / "Остановить"
- Оценка оставшегося времени: "~8 мин"

**Экран 5: Результаты**

- Сводка:
  - Всего проверено: 100
  - Не найдено в РКЛ: 97
  - Найдено в РКЛ: 2
  - Ошибки: 1
- Таблица результатов (с фильтрами: все / найденные / ошибки)
- Кнопка: "Скачать отчёт (Excel)" (крупная, синяя)
- Кнопка: "Повторить ошибочные" (если есть ошибки)
- Кнопка: "Новая проверка"

### 7.6. Размеры popup

Рекомендуемый размер popup: 400x500 px (стандартный для Chrome-расширений). При необходимости расширяется до 400x600 px на экране результатов.

---

## 8. Сервер лицензий --- подробная спецификация

### 8.1. Стек технологий

- Runtime: Node.js 20+
- Фреймворк: Express.js
- БД: SQLite (для MVP) или PostgreSQL (для продакшена)
- ORM: Prisma или Knex.js
- Хостинг: VPS (Timeweb, Selectel) или Yandex Cloud

### 8.2. API эндпоинты

**POST /api/license/activate**

Первичная активация ключа на устройстве.

Запрос:
```json
{
  "key": "RKL-XXXX-XXXX-XXXX",
  "deviceId": "uuid-generated-by-extension"
}
```

Ответ (успех):
```json
{
  "ok": true,
  "plan": "business",
  "limit": 6000,
  "used": 0,
  "expires": "2026-04-03T00:00:00Z",
  "signature": "hmac-sha256-signature"
}
```

Ответ (ошибка):
```json
{
  "ok": false,
  "error": "invalid_key | expired | already_activated"
}
```

**POST /api/license/validate**

Периодическая проверка (при каждом открытии popup + каждые 15 минут).

Запрос:
```json
{
  "key": "RKL-XXXX-XXXX-XXXX",
  "deviceId": "uuid"
}
```

Ответ:
```json
{
  "ok": true,
  "active": true,
  "plan": "business",
  "limit": 6000,
  "used": 47,
  "expires": "2026-04-03T00:00:00Z",
  "signature": "hmac-sha256-signature"
}
```

**POST /api/license/increment**

Инкремент счётчика после каждой проверки. Вызывается из background worker.

Запрос:
```json
{
  "key": "RKL-XXXX-XXXX-XXXX",
  "deviceId": "uuid",
  "count": 1
}
```

Ответ:
```json
{
  "ok": true,
  "used": 48,
  "remaining": 5952
}
```

### 8.3. Схема базы данных

```sql
CREATE TABLE organizations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  inn TEXT,
  email TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE licenses (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  key TEXT UNIQUE NOT NULL,
  organization_id INTEGER REFERENCES organizations(id),
  plan TEXT NOT NULL CHECK (plan IN ('start', 'business', 'corp', 'trial')),
  limit_per_month INTEGER NOT NULL,
  used_this_month INTEGER DEFAULT 0,
  device_id TEXT,
  activated_at DATETIME,
  expires_at DATETIME NOT NULL,
  active BOOLEAN DEFAULT TRUE,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE usage_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  license_id INTEGER REFERENCES licenses(id),
  count INTEGER NOT NULL,
  logged_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE payments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  organization_id INTEGER REFERENCES organizations(id),
  amount INTEGER NOT NULL,
  currency TEXT DEFAULT 'RUB',
  payment_id TEXT,
  status TEXT CHECK (status IN ('pending', 'completed', 'failed', 'refunded')),
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 8.4. Сброс счётчика

Cron-задача: каждый 1-го числа месяца в 00:00 МСК обнуляет used_this_month для всех активных лицензий.

### 8.5. Безопасность

- HMAC-SHA256 подпись ответов (секретный ключ вшит в расширение, обфусцирован)
- Rate limiting: не более 60 запросов/мин с одного IP
- Один ключ = одно устройство. Смена устройства --- через обращение в поддержку или кнопку "Сменить устройство" (лимит: 1 раз в 30 дней)
- HTTPS обязателен
- Логирование всех запросов (без ПДн)

---

## 9. Тарифная сетка

| Тариф | Цена/мес | Лимит проверок/мес | Целевая аудитория |
|---|---|---|---|
| Пробный | 0 руб | 10 проверок (одноразово) | Демонстрация |
| Старт | 2 990 руб | 1 500 (~50 сотр. x 30 дн.) | Малый бизнес, до 50 иностранцев |
| Бизнес | 4 990 руб | 6 000 (~200 сотр. x 30 дн.) | Средний бизнес, до 200 иностранцев |
| Корпорация | 9 990 руб | 30 000 (~1000 сотр. x 30 дн.) | Крупный бизнес, до 1000 иностранцев |

Годовая подписка: скидка 20% (2 месяца бесплатно).

---

## 10. Лендинг

### 10.1. Структура страницы

1. **Hero-секция:** "Автоматическая проверка сотрудников по РКЛ за 12 секунд. Данные не покидают ваш компьютер." + CTA "Попробовать бесплатно (10 проверок)"
2. **Проблема:** штрафы до 1 000 000 руб, ручная проверка 100 сотрудников = 8 часов, ошибки ручного ввода
3. **Решение:** расширение для Chrome, автоматизация через Госуслуги, Excel-отчёт
4. **Как это работает:** 4 шага с иллюстрациями (установить --- загрузить Excel --- нажать кнопку --- скачать отчёт)
5. **Безопасность:** данные локально, без передачи третьим лицам, не нужна ЭП
6. **Сравнение с альтернативами:** таблица (ручная проверка / api-cloud / HRonBoard / мы)
7. **Тарифы:** 3 карточки + пробный период
8. **FAQ**
9. **Регистрация и оплата**

### 10.2. Интеграция оплаты

Платёжная система: ЮKassa или Тинькофф Оплата

Требования:
- Рекуррентные платежи (автосписание ежемесячно)
- Оплата от юрлиц (выставление счёта)
- Оплата картой для быстрого старта
- Автоматическая генерация и отправка лицензионного ключа после оплаты

---

## 11. Юридические аспекты

### 11.1. 152-ФЗ --- Персональные данные

Расширение RKL Check **не является оператором персональных данных** в смысле 152-ФЗ:

- Не собирает ПДн сотрудников клиента
- Не хранит ПДн на своих серверах
- Не передаёт ПДн третьим лицам
- Данные обрабатываются исключительно в браузере пользователя
- Передача данных происходит только на Госуслуги --- государственную информационную систему, через авторизованный запрос от имени организации-работодателя

Расширение является **инструментом автоматизации** --- эквивалент макроса, который делает то же самое что кадровик делает руками, но быстрее.

Организация-работодатель (клиент) самостоятельно несёт ответственность за:
- Наличие правовых оснований для проверки сотрудников по РКЛ
- Получение согласия на обработку ПДн (или наличие иного законного основания)
- Обеспечение безопасности данных на своих устройствах

### 11.2. Оферта и условия использования

Необходимо подготовить:

1. **Публичная оферта** --- договор на оказание услуг (предоставление доступа к ПО)
2. **Политика конфиденциальности** --- описание обработки данных (email организации, платёжные данные)
3. **Условия использования расширения** --- отказ от ответственности за действия пользователя, правила использования

### 11.3. Юридическое лицо

Для приёма платежей от юрлиц и оформления оферты потребуется:
- ИП или ООО
- Расчётный счёт
- Договор с платёжной системой

---

## 12. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|---|---|---|---|
| Госуслуги изменят HTML/CSS формы | Средняя | Высокое (расширение перестанет работать) | Мониторинг страницы каждые 6 часов, алерт при изменениях, быстрое обновление content script |
| Rate limit / блокировка при массовых запросах | Низкая-Средняя | Высокое | Настраиваемая задержка 3-10 сек, случайные интервалы, лимит параллельных сессий |
| Блокировка аккаунта Госуслуг пользователя | Низкая | Высокое (для клиента) | Имитация человеческого поведения, предупреждение в документации, рекомендация не более 200 проверок за сессию |
| Google удалит расширение из Web Store | Низкая | Среднее | Дистрибуция через сайт (.crx), подготовить инструкцию по ручной установке |
| Конкурент снизит цену | Средняя | Среднее | Развивать функциональность (уведомления МВД, интеграция 1С, API для разработчиков) |
| Госуслуги добавят API для массовой проверки | Низкая | Критическое (рынок исчезает) | Переключиться на API, предложить как value-add обёртку с отчётами и историей |
| Утечка лицензионного ключа / взлом расширения | Низкая | Низкое | Device_id привязка, обфускация, серверная валидация каждого запроса |

---

## 13. План разработки

### Фаза 1 --- MVP (2-3 недели)

Цель: работающее расширение без лицензирования

Задачи:
- Настроить проект (Manifest V3, структура файлов)
- Реализовать content script (заполнение формы, чтение результата, цикл проверки)
- Реализовать popup (загрузка Excel, предпросмотр, запуск, прогресс, результаты)
- Реализовать background worker (очередь, состояние, обмен сообщениями)
- Экспорт результатов в Excel (SheetJS)
- Тестирование на реальных данных через Госуслуги
- Обработка ошибок и edge cases

Результат: расширение можно загрузить в режиме разработчика и протестировать.

### Фаза 2 --- Лицензирование (1 неделя)

Цель: контроль доступа и монетизация

Задачи:
- Развернуть сервер (Node.js + Express + SQLite)
- Реализовать 3 эндпоинта (activate, validate, increment)
- HMAC-подпись ответов
- Интегрировать проверку лицензии в расширение
- Экран активации в popup
- Блокировка при истечении подписки / исчерпании лимита

### Фаза 3 --- Лендинг и оплата (1 неделя)

Цель: начать продажи

Задачи:
- Сверстать лендинг (1 страница)
- Подключить платёжную систему (ЮKassa)
- Автоматическая генерация ключа + отправка на email
- Публикация расширения в Chrome Web Store (unlisted)
- Подготовить оферту и политику конфиденциальности

### Фаза 4 --- Развитие (по приоритету)

- Поддержка XML из 1С ЗУП
- Локальная история проверок с поиском и фильтрацией
- Уведомления при обнаружении в РКЛ (Telegram-бот, email)
- Автопроверка по расписанию (ежедневно/еженедельно)
- Автозаполнение уведомлений МВД о приёме/увольнении (как у HRonBoard)
- Мониторинг изменений вёрстки Госуслуг (автоалерт)
- Личный кабинет на сайте (управление подпиской, смена устройства)
- API для интеграции с внешними системами

---

## 14. Технические ограничения и допущения

1. Пользователь должен быть авторизован на Госуслугах как сотрудник организации до начала проверки
2. Вкладка с Госуслугами должна быть активной и видимой во время проверки
3. Скорость проверки ограничена Госуслугами --- каждая проверка занимает 5-30 секунд
4. Расширение не может работать в фоне без открытой вкладки Госуслуг
5. URL формы проверки РКЛ: `https://www.gosuslugi.ru/655781/1/form` --- SPA на React, URL не меняется между шагами
6. Поля формы: Серия (необязательное), Номер, Дата выдачи, Дата рождения. ФИО не требуется и не передаётся
7. Задержка между проверками: настраиваемая, по умолчанию 3-5 секунд (имитация человеческого поведения)
8. Формат дат на Госуслугах: ДД.ММ.ГГГГ
9. React-поля требуют заполнения через nativeInputValueSetter + dispatchEvent
10. Точная формулировка результата при нахождении в РКЛ уточняется при тестировании (на данный момент проверен только вариант "Отсутствует в реестре контролируемых лиц")
11. Поведение при неполной дате рождения (чекбокс) не тестировалось --- требует уточнения
12. Максимальная рекомендуемая партия: 200-300 сотрудников за одну сессию (для минимизации риска rate limit)
